{
  "name": "autonomous-browser-backend-python",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev:py": "uvicorn src.main:app --reload",
    "services": "poetry shell && poetry run services",
    "schedule": "poetry shell && poetry run schedule"
  }
}
